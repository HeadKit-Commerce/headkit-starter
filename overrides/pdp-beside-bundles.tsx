import type { ReactNode } from "react";

/**
 * Content beside the PDP bundles carousel. Customer-owned.
 * The page always wraps this and the bundles section in
 * `headkit-pdp-beside-bundles` so CSS can clear a floated card.
 */
export function PdpBesideBundles(): ReactNode {
  return null;
}
