{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://headkit.app/schemas/storefront-theme.json",
  "title": "HeadKit storefront theme",
  "description": "Customer-owned layout modes in overrides/. Dashboard branding still owns colors, fonts, and corners.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "Schema version for forward-compatible upgrades."
    },
    "layout": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "navLayout": {
          "type": "string",
          "enum": ["left-logo", "centered-logo", "split"],
          "description": "Primary nav logo placement."
        },
        "navStyle": {
          "type": "string",
          "enum": ["icons", "text-labels"],
          "description": "Desktop header actions presentation."
        },
        "heroLayout": {
          "type": "string",
          "enum": ["inset", "full-bleed", "fixed-height"],
          "description": "Homepage hero carousel shell."
        },
        "homepageNav": {
          "type": "string",
          "enum": ["solid", "overlay-hero"],
          "description": "Nav chrome on the homepage."
        },
        "productEnquiry": {
          "type": "boolean",
          "description": "Show the Shopify PDP product-enquiry form. Default true."
        }
      },
      "required": ["navLayout", "navStyle", "heroLayout", "homepageNav"]
    },
    "pdp": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional product-detail chrome. Starter defaults omit this.",
      "properties": {
        "sizeGuideHref": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256,
          "pattern": "^/(?!/)[A-Za-z0-9/_-]*$",
          "description": "Internal Size Guide CMS path. PDP Size Guide controls open this page in a modal (e.g. /size-guide). Direct visits still render the page."
        }
      }
    },
    "catalog": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional catalog presentation. Starter defaults omit this — only badge: tags show until a store allowlists more. Collection arrays omit = current first-N / all-roots behaviour.",
      "properties": {
        "badgeTags": {
          "type": "array",
          "description": "Product tag names or slugs that render as pills on cards and the PDP. Case-insensitive. Tags prefixed badge: always show.",
          "items": { "type": "string", "minLength": 1, "maxLength": 64 },
          "maxItems": 32
        },
        "homepageCollections": {
          "type": "array",
          "description": "Ordered collection slugs for the homepage category carousel. Omit or empty = first N from the catalog. Array order is display order.",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z0-9]+(?:-[A-Za-z0-9]+)*$"
          },
          "maxItems": 32
        },
        "shopCollections": {
          "type": "array",
          "description": "Ordered collection slugs for the Shop header carousel. Omit or empty = all root collections. Array order is display order.",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z0-9]+(?:-[A-Za-z0-9]+)*$"
          },
          "maxItems": 32
        }
      }
    },
    "copy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional section strings. Starter omits this — hardcoded titles stay. `{word}` in title uses the heading highlight face.",
      "properties": {
        "homepageFeatured": {
          "$ref": "#/$defs/sectionCopy"
        },
        "pdpBundles": {
          "$ref": "#/$defs/sectionCopy"
        },
        "pdpRelated": {
          "$ref": "#/$defs/sectionCopy"
        },
        "collectionCardLink": {
          "type": "string",
          "minLength": 1,
          "maxLength": 80,
          "description": "Link text under a homepage collection-card title. Omit = title only."
        },
        "cartNoun": {
          "type": "string",
          "enum": ["cart", "bag"],
          "description": "ATC noun. Starter default is cart. bag keeps the set-total label (Add set to bag · $total)."
        }
      }
    },
    "figma": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "fileKey": { "type": "string" },
        "referenceFrames": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        }
      }
    }
  },
  "required": ["version", "layout"],
  "$defs": {
    "sectionCopy": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional SectionHeader strings. Omit each field to keep the starter default. Empty eyebrow or allButton hides that slot.",
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1,
          "maxLength": 200,
          "description": "Section heading. `{word}` uses the heading highlight face."
        },
        "eyebrow": {
          "type": "string",
          "maxLength": 200,
          "description": "SectionHeader description slot (Velvet CSS stacks it above the title)."
        },
        "allButton": {
          "type": "string",
          "maxLength": 80,
          "description": "View-all CTA label. Empty string hides the button."
        },
        "allButtonPath": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256,
          "pattern": "^/(?!/)[A-Za-z0-9/_-]*$",
          "description": "Internal path for the View-all CTA (e.g. /featured, /collections/bundles)."
        }
      }
    }
  }
}
